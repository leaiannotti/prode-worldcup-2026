# OpenSpec Capability Registry

This directory contains the canonical (source-of-truth) specifications for capabilities in the `prode-worldcup-2026` project.

## Capabilities

| Capability | Description | Status |
|------------|-------------|--------|
| `release-pipeline` | Tag-triggered CI/CD, changelog generation, GitHub Releases, and Coolify deployment notification | Active (v1.1.0) |

## Active Changes

See `openspec/changes/` for in-progress changes.

## Archived Changes

See `openspec/changes/archived/` for completed changes.

---

*Registry maintained by SDD Archive phase.*
